# Development & CI dependencies
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Testing (kept here so runtime images stay slim)
pytest>=7.4.0
pytest-cov>=4.1.0

# Linting
ruff>=0.4.0
pypdf

# Development & CI dependencies
# Install on top of requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# Testing
pytest>=7.4,<9
pytest-cov>=4.1,<7

# Linting
ruff>=0.4

# PDF text round-trip verification (test_exports)
pypdf>=4.0
